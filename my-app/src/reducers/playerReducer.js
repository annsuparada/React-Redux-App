const initialState = {
    players : [],
    isLoading: true,
    test: 'This is a test'
}

export const reducer = (state = initialState, action) => {
switch (action.type) {
    
    default:
        return state;
}
}