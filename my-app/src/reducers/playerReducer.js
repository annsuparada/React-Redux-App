const initialState = {
    players : [],
    isLoading: false,
    test: 'This is a test'
}

export const reducer = (state = initialState, action) => {
switch (action.type) {
    
    default:
        return state;
}
}